% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{summary.confMeta}
\alias{summary.confMeta}
\title{Summarize one or more confMeta objects}
\usage{
\method{summary}{confMeta}(object, ...)
}
\value{
A data frame where each row corresponds to a method:
\itemize{
\item the main method of each \code{confMeta} object (named according to \code{fun_name})
\item comparison methods (e.g. fixed-effect, random-effects, Hartung–Knapp,
Henmi–Copas), taken from the first object
}

with the following columns:
\itemize{
\item \code{upper}, \code{lower} – bounds of the confidence interval
\item \code{AUCC}, \code{AUCC_ratio} – available only for the main methods (one row per
\code{confMeta} object)
\item \code{p_0} – p-value at \eqn{\mu = 0}
}
}
\description{
Produces a compact tabular summary of one or more \code{confMeta} objects,
including the combined confidence intervals, \eqn{p_0}, and (if available)
results from the comparison methods.

When multiple \code{confMeta} objects are supplied (e.g. with different
combination functions), their summaries are stacked in a single table.
The comparison methods are taken from the first object.
}
\examples{
# Single confMeta object
cm1 <- confMeta(estimates, SEs, fun = p_edgington, fun_name = "Edgington")
summary(cm1)

# Multiple confMeta objects (different combination functions on the same data)
cm2 <- confMeta(estimates, SEs, fun = p_other, fun_name = "Other method")
summary(cm1, cm2)

}
