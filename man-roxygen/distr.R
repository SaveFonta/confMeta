#' @param distr The distribution to use for the calculation of the p-value. Currently, the options are
#' \code{"f"} (F-distribution) and \code{"chisq"} (Chi-squared distribution). Defaults to \code{"chisq"}.
